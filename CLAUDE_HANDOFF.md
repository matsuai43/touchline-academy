# v2 アップデート設計・Claude引き継ぎ

## 制約と現在地
ユーザーの明示依頼 (2026-09-13): 選手の個性/愛着、試合中の細かな指示と声かけ成長、ゴール/セーブ映像、半年育成方針、実績に応じた多様なスカウト、応援するマネージャー、スマホUI。
Codex利用上限に達したら作業中止。別モデルや別タスクでの回避、リセット、追加購入、自動再開は禁止。Claudeへの自動委任もしない。ユーザーが引き継ぎを指示した場合にこの文書を利用。
既存公開: https://touchline-academy.pages.dev
GitHub: https://github.com/matsuai43/touchline-academy (mainへのpushでPages自動ビルド)
保存先: この専用フォルダ。既存作業は変更しない。

## 実装方針
- 既存のv1セーブを読み込み、追加データを決定的に補完する。既存選手の名前/能力/年度/試合は維持。
- オリジナルの親しみやすいデフォルメ人物画像アトラス（4x4、12選手+4マネージャー）。髪型/表情/肌/メガネ等が異なる。実在選手や既存ゲーム固有デザインを流用しない。
- 選手に顔番号、性格、経歴、信頼、積極性、献身性、思い出を持たせる。見た目だけでなく声かけの反応と成長に反映。
- 試合指示: 攻撃の経路（中央/サイド/自由）、テンポ（慎重/標準/速い）、守備ライン（低い/標準/高い）、個人の役割（自由/積極/守備）。能力と疲労に影響し、優劣と代償をUI表示。
- 試合の各15分で具体的な選手行動が発生。挑戦/戻りの遅れ/好守/疲労に対して1回だけ、ほめる/厳しく指摘/励ます/見送るを選べる。事実と性格に合う対応が成長・信頼につながる。無制限クリック育成を防ぐ。
- 実際のシミュレーション結果に紐づく得点・GKセーブ・失点を短いCanvasアニメーションで再生。映像キュー、リプレイ、一時停止/スキップ、動きを減らす対応。映像を見ただけで再度得点や成長が発生しない。映像に描く結果は必ず試合結果と一致。
- 半年=24週。技術/攻撃/堅守/走力の方針を選択・確定。期間中は同方針の練習成長補正と進捗カウント。達成目標/報酬/残期間を見える化。次期から再選択。
- スカウト: 県内原石、育成組織出身、架空プロの子、海外経験、留学生の5経歴。実在リーグ・親選手名は不使用。学校評判に応じて候補を解禁。偵察→評価判明→訪問で関係構築→内諾→4月入学。部費と週の活動回数を制限。来春18人体制を維持し、卒業枠のポジションと対応させる。未スカウト枠は通常入部。
- マネージャー: 4人から担当選択。ケア/応援/分析/スカウト補助のいずれか1つを週単位で適用。操作連打で効果が重複しない。個性のある応援文。
- UI: 新しい育成・スカウト画面。選手顔を名簿・ピッチ・プロフィール・スカウト・試合に共通表示。モバイルは44px以上の主要タップ領域、カード折返し、試合の次へボタンに到達しやすくする。

## ファイル構成（予定/実装に合わせて更新）
lib/game.ts: 既存エンジン、追加モジュールとの接続、旧セーブ移行。
lib/development.ts: 性格/半年方針/マネージャー/スカウト/声かけ/映像イベントのドメインロジック。
app/development-ui.tsx: 顔、半年方針、スカウト、マネージャー、試合指示のUI。
app/match-cinema.tsx: Canvasの短い試合演出。
app/game-ui.tsx, app/globals.css: 組込みとレスポンシブ。
public/character-atlas.png, ASSETS.md: 新画像とプロンプト・出典。
tests/: 旧セーブ、効果、制限、スカウト入学、長期シミュレーション、スマホ/試合ブラウザテスト。

## 検証・公開
npm test / npm run typecheck / npm run test:browser。
静的出力 dist/client を node scripts/serve.mjs (127.0.0.1:4173)で検証。
Windows Node24でvinext buildが全出力後libuv assertionを起こす既知問題あり。既存Cloudflare Linux builderは成功済み。成功を偽装するexit0ラッパーは作らない。
Chrome headlessは既存Playwright設定で動く。スクリーンショットも目視。セーブ移行を必ず検証。
最終的にmainへpushしてPagesビルド成功と公開URLでの操作を確認。秘密情報をチャット/ファイル/remoteに記録しない。
Git Credential Managerでmatsuai43認証可。昇格時には所有者がsandboxと異なるため必要ならそのコマンドだけ -c safe.directory=<この専用絶対パス> を使用。

## 進捗
2026-09-13: ユーザーのアップデート依頼で作業再開（自動再開ではない）。既存変更を確認。ローカルgit clean。原案作成、新画像の生成中。実装・新テスト・公開はこれから。

進捗更新: lib/development.tsと既存game.ts接続済み。旧セーブ補完、半年方針、スカウト・入部、マネージャー、声かけ、詳細指示、映像イベント。app/development-ui.tsx / app/match-cinema.tsx / game-ui.tsx組込み済。既存テスト10件と型チェック合格（UI組込後型チェックも合格）。新顔画像4x4生成済み public/character-atlas.png。追加6人の顔を生成中。CSS追加済、ブラウザQAと新機能テストはこれから。まだpushしていません。

## 2026-09-13 最終引き継ぎチェックポイント
実装済み: 上記v2機能一式。追加画像はpublic/character-atlas.png（12選手＋4マネージャー）とpublic/character-extra.png（6選手）。旧v1保存キーとversionは維持し、development.schema=2とidentityとmatch.detailsを追加。移行はvalidateSave→hydrateDevelopment→validateDevelopment。
重要な実装仕様:
- 半年方針はepoch=(season-1)*2+floor(week/24)。確定後変更不可、24週境界でリセット。報酬1回のみ。
- マネージャー活動/スカウトの週ID=season*48+week。同週の重複報酬や連打育成を禁止。
- 声かけはmatch.details.moment.answeredで1場面1回。疲労/挑戦/戻り/好守に合う対応が有効。信頼は練習の成長効率に、献身性は試合の守備に反映。
- スカウトはポジションごとの卒業枠が上限。内諾した候補が年度移行で実選手になる。未充足枠は通常入部。学校名・親や育成組織はすべて架空。
- Canvasは実際のシュート結果から作るhighlightsだけを再生。ゴール件数とスコア差の一致を20乱数シードで検証。見返してもスコア/成長は変わらない。
- 見た目は18種類を初期18選手に割り当て。次世代・候補では再利用。個人の名前、経歴、能力、性格、思い出で区別。
検証: ドメイン19件合格、型チェック合格。ブラウザ6件合格（PC/390pxスマホ、声かけ/指示/リプレイ/スカウト/旧セーブ/交代/モーダル等）。画像と画面も目視。test-resultsのスクリーンショットはgitignore（ローカルに残る）。
追加の調整: 試合進行は固定ヘッダーバーから、映像/声かけ/戦術/交代へアンカー移動。端末の動きを減らす設定では静止画。停止時Canvasの連続描画を停止しCPU消費を抑制。
既知: Windows Node24のvinextは静的生成完了後libuv assertionでexit1（v1以来、Cloudflare Linuxビルドでは正常）。lint全体は旧コード由来およびCanvas/スプライトARIA等への形式的指摘が残るため合格とは報告しない。実機スマホ/長期人手プレイ、3D連続試合、音声は範囲外。
最終公開チェックはPROGRESS.mdの末尾を参照。main push→Cloudflare自動ビルド→公開URLのブラウザテストで確認する。利用上限到達時は途中であっても中断し自動再開しない。
